import { mockRiskMapData } from '../data/mockRiskMapData'

// Replace this function with GET /risk-map when the backend spatial contract is ready.
export function getRiskMapData() {
  return mockRiskMapData
}